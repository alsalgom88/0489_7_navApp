<?xml version="1.0" encoding="utf-8"?>
<!--
    Activity per a la navegació amb Navigation Drawer.
    DrawerLayout és el contenidor principal:
      - Zona esquerra: NavigationView (menú lateral)
      - Zona central: contingut de la pantalla (NavHostFragment)
-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- ========================================================= -->
    <!-- 🟦 CONTINGUT PRINCIPAL (APP + NAVIGATION)                 -->
    <!-- ========================================================= -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <!-- ========================================================= -->
        <!-- 🟧 BARRA SUPERIOR (APPBAR + TOOLBAR)                      -->
        <!-- ========================================================= -->
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/AppTheme.Toolbar">

            <!--
                Toolbar usada com a ActionBar.
                - Mostra el títol del fragment actual
                - Gestiona el botó de hamburguesa via NavigationUI
                - Accepta menús (overflow)
            -->
            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorPrimary"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>
        </com.google.android.material.appbar.AppBarLayout>



        <!-- ========================================================= -->
        <!-- 🟩 NAVHOST: CONTINGUT QUE CANVIA SEGONS EL MENÚ DRAWER     -->
        <!-- ========================================================= -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:navGraph="@navigation/drawer_graph"
            app:defaultNavHost="true"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"/>
        <!--
            app:defaultNavHost="true"
                → rep els esdeveniments de "back" automàticament
            app:layout_behavior
                → fa que el contingut es desplaci sota l’AppBar
        -->

    </androidx.coordinatorlayout.widget.CoordinatorLayout>



    <!-- ========================================================= -->
    <!-- 🟥 MENÚ LATERAL (DRAWER)                                   -->
    <!-- ========================================================= -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/drawer_menu"
        app:headerLayout="@layout/drawer_header"/>
    <!--
        layout_gravity="start"
            → fa que aparegui a l'esquerra en orientació LTR
        headerLayout
            → capçalera personalitzada del Drawer
        menu
            → fitxer XML del menú lateral
    -->

</androidx.drawerlayout.widget.DrawerLayout>
